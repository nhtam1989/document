---
title: 'Mysql'
description: 'This is the meta description for this page'
---

```bash
CREATE SCHEMA `laravel-api` DEFAULT CHARACTER SET utf8 COLLATE utf8_unicode_ci; // Create new DB 

CREATE DATABASE `nodeExpress` DEFAULT CHARACTER SET utf8 COLLATE utf8_unicode_ci;

ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'password'; // set pass cho user
flush privileges;

show processlist;
show variables like "max_connections";

```

### Mysql Check duplicate Data
```
select   userId, announcementId, 
         count(*)
from     webtt_new.UserReadAnnouncement

group by userId, announcementId
having   count(*) > 1


```

### Nên dùng createBool để connect Mysql ( sẽ tận dụng các lệnh đang sleep )
```
const pool = mysql.createPoll({
         host:...
         ...
})


```


